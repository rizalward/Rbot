import Foundation

/// Three-language foundation for ЯOS (refined constantly in the USER MANUAL).
enum YaCode {
    /// 1) Best-understood implementation tongue on this seat (Swift here; Kotlin/JS twins later).
    static let implementation = "Swift (clay macOS seat) — clear types, offline FileManager, write-code teachable"

    /// 2) Decider tongue — how you type, speak, and order.
    static let decider = "Short orders · answer-first · NonNuclear · offline premier · bio root · no invented genotypes"

    /// 3) Я CODE — new OS language we forge for ЯOS (Ghost Claim ops + RFID token law).
    static let yaCode = """
    Я CODE v0 primitives:
    establish · claim · cite · ping/pong · transfer · settle · attest · relink · evolve
    atoms: signature→slot | two-numbers | unique-object | bytes-owned
    vehicles: Я TOKEN / Я COIN (RFID-class tag)
    carriers: local · BLE · NFC · mesh · radio · hardline · (IP optional)
    """

    static let foundation = """
    THREE LANGUAGE FOUNDATION
    1 IMPLEMENTATION — \(implementation)
    2 DECIDER TONGUE — \(decider)
    3 Я CODE — new code for ЯOS (manual living cover; refine constantly)
    Teach path: write-code → Decider review → Terminal/Rebuild. App learns to write for others and itself when you grant evolve.
    """

    static func reply() -> String { foundation }
}
