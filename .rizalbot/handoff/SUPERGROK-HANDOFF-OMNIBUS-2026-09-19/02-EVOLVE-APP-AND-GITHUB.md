# EVOLVE — app + GitHub (expeditious)

Purpose: grow clay and the public mirror **fast** without violating NonNuclear / Decider / crown-Я laws.  
Intent: every evolve step is propose→approve→seat, with purpose named first.

## App (ЯBOT clay) — evolve loop

1. **Observe** — Decider ask, TOKENBLAST verdict, or Mind note.
2. **Propose** — one Swift/JSON seat; state purpose+intent; Decider gate.
3. **Seat in localbuild** — `~/Library/Developer/ЯBOT-localbuild/` only.
4. **Smoke** — Mac `/Applications/ЯBOT.app` via `open /Applications/ЯBOT.app` (not `open -a`); iOS signed install with free disk.
5. **Lock** — append USER-MANUAL.md + Mind handoff note; never silent rename Я→R.
6. **Optional self-fix** — Decider-gated SelfFix / CODE mind drafts only; rebuild stays with human.

### Expedite levers already in clay

- `tokenblast` — verify twin after any chain change.
- SelfFix (Mac Process gated; iOS-safe) — propose Swift repairs.
- Lane chips CMD/FN/ACT/TALK — keep work vs talk separated.
- WalletCard.json + YaTwinChain.swift — single source for twin constants; update both when mint facts change.

### Do not

- Spend SOL / add pools / renounce authorities without Decider.
- Build from `~/Documents/ЯBOT` (iCloud lock).
- Invent explorer URLs or mint strings.
- Fan out agent messages or send email/Slack unasked.

## GitHub (RIZALEON/RIZALBOT) — evolve loop

1. Sync from **localbuild** tree into repo `clay/` (or project root per PR #1 layout).
2. Commit purpose-first messages; push branch `seat-wallet-landing` or successor `seat-tokenblast-handoff`.
3. Keep PR #1 updated or open successor PR; merge when Decider says.
4. Tag releases after Mac+iOS smoke: `twin-mainnet-ya`, `tokenblast-1`.
5. Never force-push `main`. Never commit secrets / keypairs from `TWIN-SOLANA/keys`.

### Suggested first GitHub seat after handoff

```
Files: TokenBlast.swift, YaTwinChain.swift, WalletLandingView.swift,
       WalletCard.json, ChainSource.json, USER-MANUAL.md,
       handoff/SUPERGROK-HANDOFF-OMNIBUS-2026-09-19/*
Branch: seat-tokenblast-handoff (or update PR #1)
```

### Remote

https://github.com/RIZALEON/RIZALBOT  
PR: https://github.com/RIZALEON/RIZALBOT/pull/1

## Standing instruction to clay (seat this in Mind)

> When SuperGrok or Decider says evolve: state purpose+intent, prefer localbuild seat, run tokenblast after twin changes, update USER-MANUAL in the same turn, push GitHub only when asked or under standing Decider order. Crown Я. Explorers are mirrors.
