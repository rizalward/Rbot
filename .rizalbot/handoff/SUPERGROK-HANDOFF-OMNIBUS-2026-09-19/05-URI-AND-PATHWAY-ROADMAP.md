# URI seat + mint → trade → pathway-clear roadmap
Sealed 2026-09-19 ~00:57 MT · Decider Rizal · Grok Bot sprint at 97% usage

## Gates
- **Gate 1 PREP:** done. File: `twin/я-metadata.json` (name/symbol **Я**, image blank).
- **Gate 2 SEND:** **WAIT** until Decider says `send URI`. No metaboss/umi, no host upload, no SOL.
- Freeze: **keep**. No renounce. No pool. No PR merge.

## After `send URI` (only then)
1. Host JSON on Decider-chosen MACHINE MIND host (do not invent).
2. `metaboss update uri` (optional name/symbol if on-chain blank) — no revoke/freeze flags.
3. `tokenblast` ONLINE → Jupiter **Я**; freeze still set; no strangeness.
4. MANUAL: "Jupiter URI seated; crown Я; freeze unchanged; explorers still mirrors."

## Seat order
A) send URI (blank image OK) → TOKENBLAST  
B) optional image URI  
C) PR #1 sync push only (no merge until ordered)  
D) freeze keep-vs-renounce Decider seat  
E) liquidity / first buy-sell with purpose+intent+amounts  
F) clear-pathway loop: clog → clay fix → optional twin mirror → TOKENBLAST

## Utility law
Clay clears clogs offline first. Public twin mirrors acts. UNIT/COIN = role labels only; crown **Я**. NonNuclear — no self-evolve, no silent SOL.
