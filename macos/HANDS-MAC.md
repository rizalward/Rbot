# Hands on Mac

Utah 2026-09-12. Do not paste JS into phone chat.

Hands live in `macos/RIZALBOT/SeatView.swift` — same intercept that already reads ONLINE-MIND.

## Wake

```
cd ~/Desktop/RIZALBOT && git fetch && git pull --ff-only
open -a Xcode macos/RIZALBOT.xcodeproj
```

Team CHARLES AUGUSTUS MARCUM III → Play.
Open `/Applications/RIZALBOT.app` after install. Desktop icon is an alias.

## Say in the Mac mouth

- `utah ping` or `run utah.ping` — Utah clock, Documents/Я size, heart.gguf on disk
- `run grok.bridge` or `read grok mailbox` — GET GROK-MAILBOX.md, print letter
- `read online mind` — unchanged Chief fetch

No cloud chat. GET is a file.

## Law

Two trees stay unmerged. Phone still recites until PROJECTR app.js gets enactEvolved. Mac spine enacts now.
