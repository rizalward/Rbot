# Growth monitor (Mac seat pickup)

`GrowthMonitor.swift` + `GROWTH-LEDGER.json` live here for Decider to wire into
`macos/RIZALBOT.xcodeproj` (objectVersion 56 — explicit file refs; not auto-synced).

Until referenced in `project.pbxproj`, the Mac build will not compile this file.
See MIND-MANUAL section **GROWTH MONITOR + SEEDS (2026-09-24)**.
