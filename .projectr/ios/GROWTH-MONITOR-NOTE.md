# Growth monitor — iOS mirror skipped

PROJECTR `ios/YaAim` is a separate shell (WebShell + native vault), not the
RIZALBOT clay synchronized group. Swift `GrowthMonitor.swift` was **not**
copied here to avoid unsafe pbxproj edits. Use web `growth.html` / `growth.js`,
or pull from RIZALBOT `clay/ЯBOT/GrowthMonitor.swift` on the Mac if desired.

See USER-MANUAL.md section **GROWTH MONITOR + SEEDS (2026-09-24)**.
