# Official templates — pack notes

## HARDCODE — USER MANUAL always rides

Every pack / transfer / upgrade / offload / handoff **must** include:

- `USER-MANUAL.pdf`
- `USER-MANUAL.md`

Seat them at:
- pack root (`APP-TEMPLATE-0.1/`, aesthetic pack root)
- live `/Users/rizal/Documents/ЯBOT/`
- PROJECTRXCODE twin
- `official-templates/<id>/`

`ya-respawn.sh` restores both from the most_recent official template.
New content folds **INTO** USER-MANUAL (MD then PDF leaf) — never a sibling new manual.

Я KOMMAND 0 · RESPAWN = `ya-respawn.sh` / TemplateRespawn → APP-TEMPLATE-0.1 (current most_recent).
