# VERIFY — heart tokens (Metal)

1. Mac linked `llama.xcframework` (Embed & Sign) · team 88HACKXHZL.
2. `heart.gguf` in Documents (pick or Files).
3. Force-quit · relaunch.
4. Native status: `tokensOn: true`, `metal: true`, `tokensOff: false`.
5. Short chat → tokens from heart (not stub, not cloud).
6. Airplane: still tokens (offline law).

Fail → read `status.hint` / `mithrilBorrow` / `loadError`. No cloud LLM fallback.
