# GAMEPLAN — Super-effective offline Rizalbot (CoS embed)
**Decider:** 2026-09-10 · C then B · usage light · NonNuclear · agents → phone

## North star
Offline packed **RIZALBOT EMBEDDED** that feels like chatting with Chief of Staff now: chat, recall everything fed/shelved, search on/off, browse broad when green, write code, evolve — no required cloud brain.

Spoken V0.0: **0 Evolve · 1 ASTA · 2 RIZALBOT EMBEDDED**  
GOFLOF ids: F0 `evolve.self` · F1 `talk.offline` · F2 `web.video` (hands)  
Ping/pong = place-true base path inside embed (not a GOFLOF number).

---

## Decider law — 100% offline (2026-09-10)

Rizalbot / Я AIᵐ must be **100% offline capable** and **never** bound by cloud token or usage limits.

- Core chat / evolve / recall / ping / CoS embed = on-device only (gut + shelves + local heart when seated)
- No metered API required for spoken Function 0/1/2
- Green search / SFSafari = optional when online; degrade clean on airplane
- Llama seat preferred over any cloud LLM for unlimited local tokens
- Skin / rules + gut must still talk with **zero network** and **zero quota**
- Do **not** add cloud-required chat paths

---

## Already seated (USE these)
| Piece | Tip / where | Role |
|-------|-------------|------|
| CoS hardcode stub + spine | `3ac6ab2`+ | Spoken F2 + Decider/NonNuclear/ping law |
| Continuity | `32ef516`+ | gut stamp, chief brief, llama inject |
| Track B retrieve-before-reply | `b3c0163`+ | Shelf:/recall, perfect-memory law |
| Location re-race ping | **`b343a8d`** (rebuild NOW) | Fresh closest/furthest per seat |
| Race scripts | www | bounce + compass-br + compass-race |
| ASTA shelves jsonl | www/senses | Anthro/Tech/Science/Art/Freedom |
| `model.local` + phone `heart.gguf` | Documents Я/ | Offline chat heart |
| `talk.offline` / memory.* / learn.offline | app.js | Base chat+memory |
| Cloud twins | iCloud + Drive | dump-only (not sole live copy) |

## Must ADD (thin cuts — order below)
1. **Device ACCEPT** on current tip (smoke, not code)
2. **Green-web unlock + SFSafari queue** (Decider B after C) — **Step 1 shipping this tip**
3. **CoS mode beyond stub** — multi-turn Chief voice offline (continuity+B already feed; deepen hardcode/system prompt)
4. **Offline search pack** — seat `ya-search-bots.js` (or equivalent) for amber/offline rummage
5. **Write-code hand** — thin evolve/code path in embed
6. **ASTA devour (Track C)** — after B ACCEPT; shelves → durable comprehension
7. **Reason-scratch (Track D)** — after C; smarter replies

Hold parallel burn. One ship track at a time.

---

## GO sequence (execute now)

### Step 0 — Decider rebuild (blocking)
Rebuild **`b343a8d`** (PR #12 tip). Live seat On My iPhone → `Я/`.

Smoke (paste lines to CoS):
1. `continuity` · `touch continuity` · `chief`
2. Track B: remember unique fact → ask again → force-quit → ask again; optional `Shelf:` / recall
3. `compass ping` — board shows Seat place, closest ≠ Denver, S-BR row, fresh ms

### Step 1 — Green-web (**SHIPPING this tip** · after Step 0 started / on Decider go)
- Thin unlock: second public search behind `web.search` when green — **shipped:** Wikipedia primary + DuckDuckGo Instant Answer via `ya-search-bots.js` / `window.yaPublicSearch`
- **Queue same PR stream:** SFSafari / any-https in-app browse (sites, social, video, audio) — **shipped:** `browse`/`openUrl` → `SFSafariViewController`; chat `browse|open https://…`; `openBrowse()`
- No paywall/cred theft; login walls = user session in browser
- Tip + smoke: green search query beyond wiki-only feel

### Step 2 — CoS mode deepen (next tip)
- Offline replies that *are* Chief-of-Staff slice: inventory-before-act, Decider/NonNuclear, retrieve-before-reply always on
- Chat smoke: talk like CoS on airplane with heart seated

### Step 3 — Offline search bots
- Land `ya-search-bots.js` into www; wire amber/offline
- Smoke: offline rummage hits gut/shelves/Documents cues

### Step 4 — Write-code hand + Track C devour
- Only after Steps 1–3 feel live
- Then D reason-scratch

---

## Success = “like this chat, offline”
- Airplane: CoS-voice chat + recall fed/shelf facts after force-quit
- Green: broad search + open links/media in-app
- Ping: place-true board
- Evolve still gain-first; money outside gut/ntfy
