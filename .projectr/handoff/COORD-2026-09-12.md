# Coord 2026-09-12 07:52 MDT

Grok connector is RIZALEON. Can write PROJECTR. Cannot write rizalward/Rbot from this session.

## Current

| Piece | Where it is newest |
|-------|--------------------|
| Mac Hands (SeatView intercept) | rizalward/Rbot `macos/RIZALBOT/SeatView.swift` |
| Mac app binary | `/Applications/RIZALBOT.app` after last Play |
| Mailbox letter | both: Rbot canonical + PROJECTR twin copy |
| LINKS doors | PROJECTR updated 07:50; Rbot LINKS older until Neo push |
| Phone `app.js` on GitHub | OLD (no enactEvolved) sha 9883a88 |
| Phone `app.js` on Neo disk | PATCHED (`await fetchTextLoose` ~line 754) |
| Phone V 0.0 search chrome | baked IPA — still old app.js |
| Phone splash no-search | old `ios/YaAim` wrap — do not overwrite V 0.0 |
| Gut | `~/Documents/Я` 4.2 KB |

## Do not copy

- Mac `SeatView.swift` / `mac.html` onto the phone
- Phone IPA onto the Mac
- Merge git histories

## Neo one-shot (Decider)

```
cd ~/Desktop/RIZALBOT && git pull --ff-only
cd ~/Desktop/PROJECTR && git pull --ff-only
sh ~/Desktop/RIZALBOT/hooks/sync-mailbox.sh
cp ~/Desktop/PROJECTR/app.js ~/Desktop/PROJECTR/ios/YaAim/www/app.js
cd ~/Desktop/PROJECTR
git add app.js ios/YaAim/www/app.js LINKS.md GROK-MAILBOX.md handoff
git commit -m "Hands app.js + twin mailbox"
git push origin main
```

Then rebuild the **search-chrome** V 0.0 target only.
