# Hands on PROJECTR

Utah 2026-09-12. Grok is now RIZALEON on this connector.

Do not merge Rbot.
Patched app.js is on the Neo at ~/Desktop/PROJECTR/app.js (await fetchTextLoose in enactEvolved).
Push that file from the Neo:

```
cd ~/Desktop/PROJECTR
git add app.js
git status
git commit -m "Hands: enactEvolved GET mailbox + folded utah.ping match"
git push origin main
```

Pages: https://rizaleon.github.io/PROJECTR/
Native V 0.0 still needs its own www rebuild. Do not install old ios/YaAim over the search chrome tile.
