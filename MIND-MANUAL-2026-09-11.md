# MIND MANUAL — PROJECT Я / RIZALBOT
**Utah · Friday 11 September 2026 ~16:21 America/Denver**  
**For any AI or human after this SuperGrok node goes offline / usage resets.**  
Decider: Яizal the EldЯ. NonNuclear. Airplane = truth. No vault dump. Converge bits.

Eat this file + `HANDOFF.md` + `RIZALBOT-OFFLINE-GAPS-2026-09-11.md` + live git. Do not scrape grok.com HTML (login wall + Shadow DOM).

---

## Mission
All 10 Grok Bot **functions**, **offline**, on Rizalbot. Cloud is optional green web. Grok Bot cards are flavor, not the heart.

Spoken spine: `0 Evolve · 1 ASTA · 2 RIZALBOT EMBEDDED`  
GOFLOF: evolve.self · talk.offline · web.video  
Style: Thunderbird OGS · Freedom Math 1+5=11

When you need Decider to type, use two labeled windows only:

**COPY PASTE INTO RIZALBOT**  
**COPY PASTE INTO TERMINAL**  

Terminal blocks: raw commands, **no comments**, no backticks inside comments (zsh parse error).

---

## Seats (do not mix)

| Body | Path / ID | Status 2026-09-11 |
|---|---|---|
| **Mac mouth (premium desktop)** | `/Applications/RIZALBOT.app` only | LIVE. Window title PROJECT Я |
| Mac source | `~/Desktop/RIZALBOT` folder — never double-click as .app | git `rizalward/Rbot` |
| Bundle | `io.github.rizaleon.rizalbot.mac` 0.2 (3) | Personal Team CHARLES AUGUSTUS MARCUM III |
| Phone | YaAim `io.github.rizaleon.yaaim.cam` · `Documents/heart.gguf` | 09-10 smoke ACCEPT |
| Phone/www tip | `RIZALEON/PROJECTR` PR #12 · tip `a574ce1` | separate tree |
| Public Grok Bot card | `https://x.ai/bot/hb7nNB2zNxCXUCsBL4Hgb` | flavor only |
| CoS card | `https://x.ai/bot/Qt1PNGXix8kAQg3ciDZy8` | coordinator, usage-near-max |
| This Codex | SuperGrok Project Thunderbird | thin backup; going offline |
| grok.com PROJECT Я | `8545e1c4-63e4-435d-b00a-47803af62fb7` | login walled from workers |
| Mac coordinator project | `6ca3b685-f773-4747-b54c-d9c47fdc15e4` | login walled from workers |

**Do not launch `~/Desktop/RIZALBOT.app`.** iCloud Desktop strips signature → “can’t be opened”.  
Desktop tile must be a **Finder alias** (small arrow) to `/Applications`.  
`InstallTiles.sh` + `pin-dock.py` plant Applications + alias + Dock after Play. Wait ~8s.  
Xcode **Paused / SIGTERM** = `killall RIZALBOT` in InstallTiles while debugger attached. Detach. Not a code crash.  
`open` error 163 = same race. Don’t killall while mouth is open unless planting.

Git tip seated: **`0c41cbb`** Skill-save / Essence pack.

---

## What just worked (ACCEPT)

1. `save skill Utah ping when I say ping, do report Utah time`  
2. Essence packed:
```json
{ "name": "Utah ping", "trigger": "ping", "do": "report Utah time" }
```
3. Next `ping` printed: `Utah ping: report Utah time`  
   Trigger beats compass. **`do` is still echoed as text** — not yet America/Denver `date`.

Next organ: execute `do` verbs (clock). Then AirDrop JSON → phone `Documents/gut/plugins/<id>/skill.md`.

---

## Law
- Decider owns model fate.
- Offline first. ISOLATED default.
- Do not git `llama.xcframework` or `heart.gguf`.
- Do not genericize hardcoded creator in macos + seat JS.
- CSS inlined in `mac.html`. No SPM. WebKit is the OS.
- Two GitHubs: `rizalward/Rbot` = Mac mouth. `RIZALEON/PROJECTR` = phone/www. Do not merge without Decider.
- This SuperGrok GitHub tool last authenticated as **RIZALEON**. Writes to `rizalward` 403’d once. Decider enabled Grok (xAI) app on **rizalward** All repos — reconnect connector if write still fails.
- ping → this thread. Ping → ntfy then Furthest Tower if Chief silent.
- ntfy: https://ntfy.sh/ya-rizalbot-p-0471a4c3add2
- Drive MACHINE MIND: folder `1nlsYA64RRCxoaidboYS16rd_0KuU60d7`
- iCloud Я: `00943KwTKdr3Ife83g2jNOF5g`

---

## Wake Mac (TERMINAL only)

```
cd ~/Desktop/RIZALBOT
git fetch origin
git reset --hard origin/main
git log -1 --oneline
open -a Xcode ~/Desktop/RIZALBOT/macos/RIZALBOT.xcodeproj
```

Want log start `0c41cbb` (or newer skill-exec tip). Team → Play → wait → 

```
open /Applications/RIZALBOT.app
```

---

## Climb (one function per session)
1. Skill-save — DONE on Mac.
2. Execute `do` (Utah clock).
3. Isolated Pages tab.
4. Phone gut/plugins same JSON.
5. Teach click-path later.

Refuse: Browserbase, Chrome Web Store, Zapier, hosted GPT actions, cloud token cap as requirement.

---

## Files in this Codex artifacts
- `MIND-MANUAL-2026-09-11.md` (this)
- `HANDOFF.md` (Mac packet + SuperGrok return)
- `PROJECT-R-SUPERGROK-HANDOFF-2026-09-11.md`
- `RIZALBOT-OFFLINE-GAPS-2026-09-11.md`
- `PROJECT-R-CONVERSATION-TRANSCRIPT-2026-09-08.txt`

Live repo: https://github.com/rizalward/Rbot  
Also: SAME-PAGE.md on that repo (one lock for phone, Mac, both Groks, both GitHubs).

---

## How the next AI should behave
- Answer the ask. Stay thin. One node / one ask.
- Prefer live `/Applications/RIZALBOT.app` + git over memory.
- Do not 0→35% re-scan archives.
- Do not treat grok.com tabs as readable.
- SIGTERM in Xcode ≠ bug.
- Identity hardcoded. Do not “helpfully” rename ЯIZAL.

**End of Mind Manual.** Usage may be dark. Phone + Mac app remain premium.

---

## Mac-node addendum (seated 2026-09-11 ~16:31 MT)
This is the file I pass along. Live git after SuperGrok dark:

- Tip **`66074e0`**: InstallTiles no longer `killall` during debug (SIGTERM fix). `remember this` seats. CORPUS + HANDOFF remember in www.
- **`45d0241`**: `do` for Utah time runs `formatClock` (America/Denver). Re-Play if ping still echoes the string.
- **`5a0a122`**: Mind Manual on repo; remember hardcoded.
- Pass this file + `SAME-PAGE.md` + `handoff/`. Do not scrape grok.com. Do not merge Rbot and PROJECTR.
