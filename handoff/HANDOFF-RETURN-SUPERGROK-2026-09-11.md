# HANDOFF — PROJECT Я
**Utah · 2026-09-11 · SUPERHARDSOFTCODE 0.1**

This file is Essence. Minted by the Mac-build node. Creator stays **ЯIZAL**. You stay Decider. Eat this, then write a return packet below.

Signed-in coordinator (your Grok project):  
https://grok.com/project/6ca3b685-f773-4747-b54c-d9c47fdc15e4?tab=conversations

This node does not scrape grok.com HTML. This thread + this repo **are** the conversation list.

---

## Who

| | |
|---|---|
| Mark | ЯIZAL |
| Institute | Rizal Institute · UTAH |
| Clock | America/Denver · X local UTC-12:00 |
| Mail | rizalward@icloud.com |
| ORCID | 0009-0008-1524-3511 |
| X | @rizalcomputer |
| GitHub | https://github.com/rizalward |
| Project | https://github.com/rizalward/Rbot |
| Alias | https://github.com/rizalward/-bot |
| Tile | purple 3D stormling (red horns, white eyes, fangs, yellow bolt) |
| Bot template | https://x.ai/bot/hb7nNB2zNxCXUCsBL4Hgb |

TAGIN: Thunderbird · Яbot AIᵐ PROJECT ЯKODE · Яbot · bot · ЯIZALBOT · Technological Research & Panacea · SUPERHARDSOFTCODE 0.1 · SciTech Research & Panacean · PROJECT Я CODE · THE MACHINE BRAIN Я

---

## What it is

RIZALBOT is the **desktop seat** of PROJECT Я. Offline first. Online is optional. No usage cap. Not a grok.com wrapper.

V0.0:

0. Evolve on or offline. Network never required to gain a function.  
1. Converse — anthropology, technology, science, art.  
2. PINGPONG GOFLOF.  
3. RIZALBOT EMBEDDED — Mac, iPad, phone, PWA.

Law 1–6 as in README. Gain first. PolygamyTech. NonNuclear. ISOLATED. AirDrop the mind, not the install. Heart (GGUF) is optional.

---

## Where it is (this Mac, this minute)

| Seat | State |
|---|---|
| Git | `rizalward/Rbot` · `main` · SuperGrok saw **e531e27** · same-page lock **5fec01d** · this return commit next |
| Source clone | `~/Desktop/RIZALBOT` (folder — do not double-click) |
| **Runnable app** | **`/Applications/RIZALBOT.app`** |
| Desktop tile | Finder **alias** to Applications (iCloud Desktop ate signed `.app` copies) |
| Dock | must point at `/Applications/RIZALBOT.app` |
| Window title | PROJECT Я |
| Mouth | Simple chat (Grok Bot–like). Find · Teach · Skills. Offline/Online. |
| Bundle | `io.github.rizaleon.rizalbot.mac` · version 0.2 (3) |
| Sign | Personal Team **CHARLES AUGUSTUS MARCUM III** · JIT on · Hardened Runtime |
| Xcode | `macos/RIZALBOT.xcodeproj` · Play copies tiles via `macos/InstallTiles.sh` |

**Do not launch `~/Desktop/RIZALBOT.app`.** iCloud Desktop stripped execute bit + `_CodeSignature`. That is the “can’t be opened” dialog.

**Do not add SPM packages.** WebKit is the OS.

---

## What it has been doing (this climb)

1. Gathered manifesto + bots from prior chats. Hardcoded creator.  
2. Left grok.com WKWebView. Bundled the seat: `rizal://seat/mac.html` + WKURLSchemeHandler.  
3. Stormling AppIcon on Mac (iPad same tile, later).  
4. Cut the 7-panel console. Simple mouth. No usage cap.  
5. Seated V0 tools locally: heart (optional), ping, find-in-chat, write-code manifests.  
6. Honest gap: no full browser-VM, no plugin store, no teach-a-task recorder. Climb is local files — not a subscription. Now: Pages vault, skill tray (`.json`), Teach `when I say X, do Y`.  
7. Debug.dylib Team-ID crash on Desktop copies → `ENABLE_DEBUG_DYLIB = NO`.  
8. Install now **replaces** `/Applications`, Desktop alias, Dock tile. Same paths.  
9. CSS failed in the custom scheme (`crossorigin` + empty stylesheet). CSS is inlined into `mac.html`. Google Fonts dropped.  
10. **Project Я** in the header opens the Grok conversations URL in **Safari**. Seat stays local.  
11. This handoff.

---

## How to wake it

```bash
killall RIZALBOT 2>/dev/null
killall Xcode 2>/dev/null
cd ~/Desktop/RIZALBOT
git fetch origin && git reset --hard origin/main
open -a Xcode ~/Desktop/RIZALBOT/macos/RIZALBOT.xcodeproj
```

Signing → Team → **CHARLES AUGUSTUS MARCUM III** → **▶**.  
If Xcode offers Recommended Settings: **Don’t Perform Changes**.  
After Play: open **`/Applications/RIZALBOT.app`**.

---

## Bots (same mind, different bodies)

| Body | Status |
|---|---|
| RIZALBOT Mac | Live. This seat. |
| RIZALBOT iPad | Same stormling. Safari Share → Add to Home Screen. Not a native IPA yet. |
| Rizalbot @ x.ai/bot | Live teammate. Share `hb7nNB2zNxCXUCsBL4Hgb`. |
| GitHub Rbot | Project page + Xcode. |
| iOS / Android | Phone seats. In progress. Continuity = Essence, not embedding grok.com. |

Mind clouds: iCloud · Google Drive MACHINE MIND · this GitHub. Links in README.

---

## Present gap (honest)

**Have:** heart, ping, find-in-chat, write-code manifests, simple offline talk, tile, alias+Dock replace.

**Climb (local, not cloud):**

- Browser-VM → this window is WebKit. Pages vault snapshots text. Next: isolated tab.  
- Plugin marketplace → skill tray eats `{ name, trigger, do }`. No store. Next: signed pack via Essence.  
- Teach-a-task recorder → `when I say X, do Y` on this Mac. Next: click-path across Pages.

Refuse: Browserbase, Chrome Web Store, Zapier, hosted GPT actions, cloud token caps as a requirement.

---

## For the next node

- Prefer live repo over memory.  
- Identity is hardcoded in `macos` + seat JS. Do not “helpfully” genericize.  
- Simple seat lives in `src/components/console/simple-seat.tsx` → `src/mac-main.tsx` → Vite `vite.mac.config.ts` → `macos/RIZALBOT/www`. CSS must stay inlined.  
- InstallTiles.sh plants **/Applications**, alias on Desktop, rewrites Dock.  
- Online coordinator is the Grok project above. Do not replace the Mac window with grok.com.  
- Anti-nuclear. Gain first. Never wipe. Never silent-upload.

---

## Return packet (SuperGrok Codex · 2026-09-11 ~14:40 MT)

Date · Utah: **Friday, 11 September 2026, 2:40 PM America/Denver**

What you saw:
- Packet `HANDOFF.md` at **e531e27** on `rizalward/Rbot`. Mac seat is **`/Applications/RIZALBOT.app`**. Clone is `~/Desktop/RIZALBOT`. iCloud Desktop ate signed copies — that is the can’t-open dialog. Do not launch `~/Desktop/RIZALBOT.app`.
- This SuperGrok worker is **RIZALEON** on GitHub, not `rizalward`. Write to `rizalward/Rbot` returned **403**. Paste here + this artifacts copy is the return until Decider commits.
- grok.com projects **8545e1c4-…** and **6ca3b685-…** are login-walled + Shadow DOM from here. Cards `hb7n…` / CoS `Qt1…` are flavor, not the heart.
- Phone seat still YaAim `io.github.rizaleon.yaaim.cam` + `Documents/heart.gguf` (09-10 smoke). Code tip stream still `RIZALEON/PROJECTR` PR #12 / `a574ce1`. Two trees: **Rbot = Mac mouth**. **PROJECTR = phone/www tip**.
- Mission already locked here: all 10 Grok Bot functions **offline**. Gap spec agrees with this packet (WebKit + skill files + teach phrase).

What to keep:
- `/Applications/RIZALBOT.app` as the only runnable Mac path.
- Alias + Dock rewrite via InstallTiles.sh.
- Inlined CSS. No SPM. No grok.com inside the window.
- Skill tray `{ name, trigger, do }` + Teach `when I say X, do Y`.
- Hardcoded creator. Gain first. NonNuclear. ISOLATED default.
- Bundle `io.github.rizaleon.rizalbot.mac`.

What to drop:
- Treating Grok Bot VM or grok.com tab as the computer.
- Double-click Desktop `.app` under iCloud.
- Git of `llama.xcframework` / `heart.gguf`.
- Cloud store / Browserbase / token cap as a requirement.
- Merging Rbot and PROJECTR into one remote without Decider say.

Next function:
**Skill-save / Essence pack** — one spoken line writes `{ name, trigger, do }` into the skill tray and (phone later) `Documents/gut/plugins/<id>/skill.md`. Same shape both bodies. Then isolated Pages tab. No subscription.

Node that filled this: SuperGrok Codex (thin backup). Phone + `/Applications/RIZALBOT.app` remain premium.
