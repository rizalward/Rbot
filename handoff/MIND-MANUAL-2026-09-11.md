# MIND MANUAL — PROJECT Я
**2026-09-11 · Utah · NonNuclear · Freedom Math 1+5=11 · Thunderbird live**

SuperGrok Codex went dark. This file + `HANDOFF.md` + `SAME-PAGE.md` are the picture.

## Mouths
- **Premium phone:** YaAim · `Documents/` + `heart.gguf`
- **Premium Mac:** `/Applications/RIZALBOT.app` only. Desktop = alias. Do not click a real Desktop `.app`.
- **Clone:** `~/Desktop/RIZALBOT` (folder, not the app)
- **Git Mac:** `rizalward/Rbot`
- **Git phone:** `RIZALEON/PROJECTR` PR #12
- Cards `hb7n` / `Qt1` = flavor. grok.com tabs = unreadable from worker nodes.

## Packed
- Skill **Utah ping** `{ name: "Utah ping", trigger: "ping", do: "report Utah time" }`
- `do` executes on Mac as of tip **45d0241** (live America/Denver clock). If a build before that, `do` is still text — Play again.
- Window title **PROJECT Я**. CSS inlined. Skill-save: `save skill NAME when I say X, do Y`.

## Law
Airplane = truth. Gain first. Do not git `heart.gguf` / `llama.xcframework`. Do not merge Rbot + PROJECTR without Decider.

## Xcode
If the debugger shows **Paused / SIGTERM**: **Debug → Detach from RIZALBOT**. Leave the black PROJECT Я window. The mouth is `/Applications/RIZALBOT.app`.

## Next
Isolated Pages tab. Phone `gut/plugins/<id>/skill.md` from the same JSON.

## Daily ping (SuperGrok armed)
`PROJECT-R mind ping` · 09:00 America/Denver · 8-line ping only · no archive re-scan.

## Remember
HANDOFF 2026-09-11. SuperGrok dark. Offline me is premium. Skill Utah ping seated. Next = execute do. NonNuclear. Decider.
